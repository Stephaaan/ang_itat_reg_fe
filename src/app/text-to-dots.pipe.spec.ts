import { TextToDotsPipe } from './text-to-dots.pipe';

describe('TextToDotsPipe', () => {
  it('create an instance', () => {
    const pipe = new TextToDotsPipe();
    expect(pipe).toBeTruthy();
  });
});
