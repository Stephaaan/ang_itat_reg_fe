import { DisplayAccomodationPipe } from './display-accomodation.pipe';

describe('DisplayAccomodationPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayAccomodationPipe();
    expect(pipe).toBeTruthy();
  });
});
